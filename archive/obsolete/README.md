# Obsolete Desktop Archive

This directory keeps removed desktop files that are not active in the Wayland
profile but should not be lost from git history.

Use `archive/x11/` for old i3, Polybar, rofi, picom, or other X11 session files.
Use this directory for retired launchers, temporary profiles, and removed helper
scripts that are no longer deployed by `home -y`.
