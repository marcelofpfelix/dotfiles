#!/usr/bin/env bash

################################################################################
#                                 FILE INFO
# custom docker exec with shell selection
################################################################################

# change to local directory
cd "$(dirname $0)"
# load libs
. ../lib/lib_utils.sh
. ../lib/lib_de.sh

main() {
  de "$@"
}

  # $1: container name
main "$@"
